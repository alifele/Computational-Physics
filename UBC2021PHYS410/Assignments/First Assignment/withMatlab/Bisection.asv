function result = Bisection(F, interval, max_iteration)

    a = interval(1);
    b = interval(2);
    
    for i=[1:max_iteration]
        
        if (F((a+b)/2).*F(b) > 0)
            b = (a+b)/2;
        else
            a = (a+b)/2;
        end
    
    end

    result = (a+b)/2

end


